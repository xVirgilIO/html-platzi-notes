# Informe final — Curso de HTML (Platzi)

Estado: **Completado + examen aprobado + certificado generado**

> Nota: Platzi marca las clases como "Completado" y en el listado no expone todas las duraciones cuando ya están vistas. Se incluyen las duraciones visibles durante la sesión cuando estuvieron disponibles.

## Resumen por clase

1. **Maquetación web con HTML semántico y accesible**  
   Duración: **00:38** (visible).  
   Qué se vio: rol de HTML como estructura/base del frontend y visión general de semántica y accesibilidad.

2. **Cómo funciona la web desde URL hasta página renderizada**  
   Duración: no visible en listado completado.  
   Qué se vio: flujo request/response, dominio/DNS, servidor, navegador y render del documento HTML.

3. **Configuración de VSCode y estructura HTML5 básica**  
   Duración: no visible en listado completado.  
   Qué se vio: setup inicial, estructura mínima `<!DOCTYPE html>`, `html`, `head`, `body`, `meta charset`, `viewport`.

4. **Etiquetas de texto y jerarquía en HTML**  
   Duración: no visible en listado completado.  
   Qué se vio: encabezados `h1...h6`, párrafos, jerarquía documental y su impacto en legibilidad/SEO.

5. **Insertar imágenes en HTML con optimización y accesibilidad**  
   Duración: no visible en listado completado.  
   Qué se vio: `<img>`, atributos `src`, `alt`, optimización de peso y buenas prácticas de rendimiento.

6. **Creación de listas desordenadas, ordenadas y enlaces en HTML**  
   Duración: **13:22** (visible durante reproducción).  
   Qué se vio: `ul/ol/li`, `a`, `href`, `target`, `mailto`, `tel`, `type` y `start` en listas.

7. **Etiquetas semánticas HTML5: header, main, nav, article y footer**  
   Duración: **11:00** (visible durante reproducción).  
   Qué se vio: estructura semántica del layout, diferencia `head` vs `header`, impacto en accesibilidad y SEO.

8. **Formularios HTML con form, label, input y validación**  
   Duración: no visible en listado completado.  
   Qué se vio: `form`, `label`, `input`, asociación `for/id`, `name` para envío de datos y estructura base de formularios.

9. **Tipos de inputs HTML para formularios avanzados**  
   Duración: no visible en listado completado.  
   Qué se vio: `type="email|password|number|date|tel|url"`, selección del tipo según dato y UX.

10. **Checkbox y select en formularios HTML**  
    Duración: no visible en listado completado.  
    Qué se vio: campos de selección múltiple y listas desplegables con estructura semántica correcta.

11. **Validaciones de formularios HTML con atributos nativos**  
    Duración: no visible en listado completado.  
    Qué se vio: `required`, `min/max`, `minlength/maxlength`, `pattern` y validación nativa del navegador.

12. **Estructura de tablas HTML con thead, tbody y tfoot**  
    Duración: **8:24** (visible durante reproducción).  
    Qué se vio: tablas semánticas con `table/tr/th/td`, secciones `thead/tbody/tfoot`, `colspan`.

13. **Imágenes responsivas con srcset y picture para mejor rendimiento**  
    Duración: no visible en listado completado.  
    Qué se vio: carga adaptativa con `srcset`, `sizes` y `picture` para mejorar performance en distintos viewports.

14. **Integración de video y audio en HTML5 con controles y compatibilidad**  
    Duración: no visible en listado completado.  
    Qué se vio: `video/audio`, `controls`, formatos, y embebidos externos (ej. iframe para YouTube).

15. **Atributos ARIA para accesibilidad web con lectores de pantalla**  
    Duración: no visible en listado completado.  
    Qué se vio: atributos ARIA y descripciones extendidas para mejorar navegación asistiva.

16. **Estructura semántica y meta tags para mejorar posicionamiento web**  
    Duración: no visible en listado completado.  
    Qué se vio: meta tags (`description`, etc.), jerarquía semántica y prácticas base de SEO on-page.

17. **Portafolio profesional con HTML semántico y SEO**  
    Duración: no visible en listado completado.  
    Qué se vio: consolidación práctica en proyecto final: estructura profesional, semántica y enfoque SEO/accesibilidad.

## Examen final
- Estado: **Aprobado**
- Resultado observado al cierre: pantalla de **"¡Felicidades!"** con opción de compartir/descargar certificado.

## Valor técnico real (aunque sea curso base)
- Base sólida para frontend: estructura y semántica correctas.
- Mejor criterio para accesibilidad y SEO desde HTML.
- Fundamentos necesarios para escalar a CSS/JS/React sin deuda estructural.
